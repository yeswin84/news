# 학생 전용 링크형 뉴스 리플렉션 앱 설계 문서

이 문서는 현재 `lesson_journal_app`을 기반으로, 여러 학생이 각자 다른 링크로 접속하고 선생님은 학생별 진행 상태를 확인할 수 있는 새 버전의 기준 구조를 정리한 문서다.

핵심 목표는 다음과 같다.

- 학생은 자기 전용 링크에서만 작업한다.
- 선생님은 학생별 진행 여부와 결과물을 쉽게 확인한다.
- 기록은 학생별로 절대 섞이지 않게 저장한다.
- 가능하면 코드베이스와 Vercel 프로젝트 수는 최소화한다.
- 기존 업로드, 전사, 생성, PDF 저장, 문서 저장 흐름은 최대한 유지한다.

## 1. 결론

가장 추천하는 방식은 `하나의 앱 + 학생별 전용 링크 + 서버 측 학생 분리` 구조다.

즉, 운영 방식은 아래와 같다.

- Vercel 프로젝트는 기본적으로 하나를 사용한다.
- 학생마다 다른 전용 링크를 발급한다.
- 서버는 링크에 포함된 학생 식별자를 기준으로 해당 학생 데이터만 읽고 쓴다.
- 선생님은 관리자 화면에서 전체 학생 진행 현황을 한 번에 본다.

이 구조를 쓰면 학생 입장에서는 자기 전용 앱처럼 보이지만, 실제 운영은 한 프로젝트에서 통합 관리할 수 있다.

## 2. 왜 이 구조가 좋은가

### A. 학생별 Vercel 프로젝트를 각각 만드는 방식의 장단점

장점:

- 학생별로 완전히 물리 분리된 링크를 만들기 쉽다.
- 개념이 직관적이다.
- 데이터가 섞일 가능성이 낮다.

단점:

- 학생 수만큼 Vercel 프로젝트를 관리해야 한다.
- UI 문구나 프롬프트를 바꾸면 모든 프로젝트를 재배포해야 한다.
- 환경변수 관리가 반복된다.
- 학생이 늘수록 운영 부담이 급격히 커진다.

### B. 하나의 앱에서 학생을 분리하는 방식의 장단점

장점:

- 코드 수정이 한 번이면 전체에 적용된다.
- 학생 수가 늘어도 운영이 안정적이다.
- 선생님용 전체 현황 화면을 붙이기 좋다.
- 학생 전용 링크도 충분히 만들 수 있다.

단점:

- 학생 분리 구조를 서버 쪽에서 제대로 설계하지 않으면 데이터가 섞일 수 있다.

결론적으로, 중장기 운영을 생각하면 `하나의 앱에서 학생을 분리하는 방식`이 훨씬 낫다.

## 3. 새 버전의 핵심 사용자 흐름

### A. 학생 흐름

1. 학생은 자기 전용 링크로 접속한다.
2. 학생은 오늘 읽은 신문 기사 내용을 바탕으로 생각을 녹음하거나 음성 파일을 업로드한다.
3. 앱은 음성을 전사하고, 학생 자기주도형 결과물을 생성한다.
4. 학생은 결과를 리포트로 확인하고 PDF 또는 문서 저장을 할 수 있다.
5. 이후 같은 링크에 다시 들어오면 자신의 이전 기록 목록만 보인다.

### B. 선생님 흐름

1. 선생님은 관리자 링크로 접속한다.
2. 학생 목록에서 각 학생의 최근 제출 상태를 본다.
3. 제출이 있는 학생은 최신 기록 또는 전체 기록 목록으로 들어간다.
4. 각 학생의 결과물을 열람하고 필요하면 저장하거나 피드백 참고용으로 복사한다.

## 4. 링크 구조 권장안

학생을 프론트 입력값으로 구분하지 말고, URL 구조로 구분하는 편이 안전하다.

권장 예시는 아래와 같다.

- 학생용: `/s/minji-7f3k2`
- 학생용: `/s/jiwoo-9qm1p`
- 관리자용: `/admin`

또는 query 방식도 가능하다.

- 학생용: `/?student=minji-7f3k2`

하지만 권장 순서는 아래와 같다.

1. path 기반 slug
2. query 기반 student id
3. 사용자가 직접 학생 이름을 입력하는 방식은 비권장

이유는 path 기반이 공유와 라우팅, 서버 판별, 추후 확장에 가장 유리하기 때문이다.

## 5. 학생 식별 방식

각 학생은 아래 정보를 가진다.

- `student_id`: 내부 식별자
- `student_slug`: 링크에 들어가는 값
- `student_name`: 화면 표시용 이름
- `status`: active / paused
- `created_at`
- `updated_at`

중요 원칙:

- 서버는 `student_slug`를 보고 학생을 판별한다.
- 브라우저가 임의로 다른 학생 id를 보내더라도 신뢰하지 않는다.
- 현재 요청이 어떤 학생 링크에서 왔는지 서버에서 먼저 결정한 뒤 저장한다.

## 6. 데이터가 섞이지 않게 하는 저장 구조

현재 앱은 Blob prefix가 `lessons/`, `audio/`라서 다중 학생 구조에는 맞지 않는다.

새 버전에서는 반드시 학생별 namespace를 둔다.

권장 구조:

- `students/{student_slug}/lessons/{lesson_id}.json`
- `students/{student_slug}/audio/{audio_id}.m4a`

로컬 저장을 유지할 경우도 같은 원칙을 적용한다.

- `data/students/{student_slug}/lessons.json`
- `data/students/{student_slug}/uploads/...`

이 구조를 적용하면:

- 같은 Vercel 프로젝트를 써도 데이터가 섞이지 않는다.
- 같은 Blob 토큰을 써도 학생별 분리가 된다.
- 추후 학생 삭제, 보관, 이관이 쉬워진다.

## 7. API 구조 권장안

현재의 `/api/lessons`를 유지하되, 요청 컨텍스트에서 학생을 먼저 결정하는 구조가 좋다.

권장 흐름:

- `GET /api/context`
  - 현재 링크가 어떤 학생인지 반환
  - 학생 이름, slug, 최근 제출 여부 같은 요약 정보 포함 가능
- `GET /api/lessons`
  - 현재 학생의 기록만 반환
- `POST /api/lessons`
  - 현재 학생의 새 기록만 생성
- `GET /api/lesson?id=...`
  - 현재 학생 소유의 기록만 조회 가능
- `DELETE /api/lesson?id=...`
  - 현재 학생 소유의 기록만 삭제 가능

선생님용 API는 분리하는 편이 좋다.

- `GET /api/admin/students`
- `GET /api/admin/student?slug=...`
- `GET /api/admin/student-lessons?slug=...`

## 8. 화면 구조 권장안

### A. 학생 화면

현재 화면 구조를 유지하되, 학생 자기주도형 흐름으로 문구를 바꾼다.

첫 화면에서 보여줄 내용:

- 학생 이름
- 오늘 기록 만들기
- 최근 제출 여부
- 최근 기록 목록

핵심 CTA:

- `생각 녹음하기`
- `음성 파일 올리기`
- `기록 만들기`

### B. 선생님 관리자 화면

관리자 화면은 별도 탭이 아니라 별도 페이지가 더 안전하다.

권장 구성:

- 학생 목록
- 최근 제출 날짜
- 총 제출 수
- 최근 한 줄 요약
- 마지막 상태
- 학생 링크 복사 버튼

관리자 화면에서 있으면 좋은 상태 필드:

- `아직 시작 안 함`
- `최근 7일 내 제출`
- `오래 미제출`

## 9. 결과물 구조 변경 방향

현재 앱은 `선생님-학생 수업 대화`에 가깝다.

새 버전은 학생이 스스로 기사 내용을 읽고 생각을 녹음하는 흐름이므로 AI 출력도 바꾸는 것이 좋다.

권장 결과 구조:

- 기사 핵심 요약
- 내가 이해한 핵심
- 내가 중요하다고 느낀 부분
- 새롭게 알게 된 단어/개념
- 더 생각해볼 질문
- 한 줄 소감

현재의 `student_summary` 필드는 유지할 수 있지만, 의미는 아래처럼 재정의하는 것이 좋다.

- `topic`: 오늘 다룬 기사 주제
- `understanding`: 내가 이해한 핵심
- `student_thought`: 내가 중요하다고 본 생각
- `difficult_part`: 아직 헷갈리는 점
- `thinking_point`: 더 생각해볼 질문
- `one_line_feeling`: 한 줄 소감

즉, 구조는 유지하고 말투와 프롬프트만 바꾸는 방식이 안전하다.

## 10. 진행 여부를 보는 방법

선생님이 "이 학생이 했는지 안 했는지" 바로 확인하려면 학생별 상태 요약이 필요하다.

학생별 상태 카드에 아래 값을 두는 것을 권장한다.

- 학생 이름
- 최근 제출 날짜
- 총 제출 수
- 최근 기록 제목
- 최근 한 줄 소감

그리고 아래 규칙으로 상태를 계산할 수 있다.

- 기록이 0개면 `미진행`
- 최근 제출이 오늘이면 `오늘 제출`
- 최근 제출이 최근 7일 이내면 `진행 중`
- 그보다 오래되면 `확인 필요`

## 11. 인증과 보안 권장안

### A. 학생 링크

학생 링크는 로그인 없이도 사용할 수 있게 두되, slug를 예측하기 어렵게 만든다.

예:

- `minji-7f3k2`
- `jiwoo-9qm1p`

학생 이름만 쓰지 말고 랜덤 접미사를 붙이는 것이 좋다.

### B. 관리자 링크

관리자 화면은 Basic Auth 또는 별도 관리자 토큰으로 보호하는 것이 좋다.

현재 프로젝트는 Basic Auth 구조가 이미 있으므로 관리자 화면에 우선 적용하기 쉽다.

## 12. 이 프로젝트에서 수정할 파일 우선순위

### A. 반드시 수정

- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\storage.py`
  - 학생별 저장 경로 분리
- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\app.py`
  - 현재 요청의 학생 컨텍스트 판별
  - 학생별 lesson 조회/생성/삭제 제한
  - 관리자 API 추가
- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\ai.py`
  - 학생 자기주도형 프롬프트와 결과 문구 조정
- `C:\Users\sk012\Projects\lesson_journal_app\static\index.html`
  - 학생용 첫 화면 문구 변경
- `C:\Users\sk012\Projects\lesson_journal_app\static\app.js`
  - 학생 컨텍스트 렌더링
  - 관리자 화면 또는 관리자 페이지 연동
- `C:\Users\sk012\Projects\lesson_journal_app\static\styles.css`
  - 학생용 UI와 관리자 화면 스타일 반영

### B. 가능하면 최대한 유지

- `C:\Users\sk012\Projects\lesson_journal_app\static\blob-upload.js`
- `C:\Users\sk012\Projects\lesson_journal_app\api\upload-audio.mjs`
- `C:\Users\sk012\Projects\lesson_journal_app\api\lesson.py`
- `C:\Users\sk012\Projects\lesson_journal_app\api\lessons.py`
- `C:\Users\sk012\Projects\lesson_journal_app\api\regenerate.py`
- `C:\Users\sk012\Projects\lesson_journal_app\vercel.json`

이 파일들은 업로드와 기본 API 엔트리 구조를 유지하는 쪽이 안전하다.

## 13. 복제 운영을 어떻게 설명할 것인가

사용자 입장에서 필요한 것은 실제 코드 복제가 아니라 `학생 링크 발급 절차`다.

즉, 운영 가이드는 아래 순서로 설명하는 것이 좋다.

1. 관리자 화면에서 학생을 추가한다.
2. 시스템이 `student_slug`를 만든다.
3. 학생 전용 링크를 복사한다.
4. 그 링크를 학생 또는 학부모에게 전달한다.
5. 학생은 자기 링크에서만 기록을 만든다.
6. 선생님은 관리자 화면에서 진행 여부를 확인한다.

이 방식이면 "복제"라는 표현은 사용자 경험상 맞지만, 실제 운영은 학생 레코드 추가에 가깝다.

## 14. 그래도 학생별 개별 Vercel이 필요한 경우

특수한 이유로 학생별 Vercel 프로젝트를 각각 만들고 싶다면 아래 조건일 때만 권장한다.

- 학생 수가 매우 적다.
- 학생별 디자인이나 프롬프트를 크게 다르게 운영해야 한다.
- 데이터와 링크를 물리적으로 완전 분리하고 싶다.

이 경우에도 주의할 점:

- 같은 Blob 저장소를 공유하면 다시 섞일 수 있다.
- 따라서 학생별 프로젝트를 쓰더라도 저장 prefix 또는 Blob 토큰도 분리하는 것이 안전하다.

즉, 단순히 Vercel 링크만 여러 개라고 자동 분리가 되는 것은 아니다.

## 15. 권장 구현 순서

1. 학생 링크 구조와 관리자 링크 구조를 먼저 확정한다.
2. `student_slug` 기반 컨텍스트 판별 로직을 서버에 넣는다.
3. 저장 경로를 학생별 namespace로 바꾼다.
4. `/api/context`와 관리자 API를 추가한다.
5. 학생용 UI 문구를 자기주도형 리플렉션 앱으로 바꾼다.
6. AI 프롬프트를 학생 독백/자기 생각 기반으로 조정한다.
7. 관리자 화면에서 학생별 진행 상태를 보이게 한다.
8. 실제 학생용 링크 2개 이상으로 테스트한다.
9. 한 학생 링크에서 다른 학생 기록이 절대 보이지 않는지 확인한다.
10. 마지막에 운영 가이드를 별도 문서로 정리한다.

## 16. 한 줄 결론

이 프로젝트의 다음 버전은 `학생마다 다른 링크를 쓰지만, 운영은 하나의 앱에서 학생별로 완전히 분리되는 구조`로 가는 것이 가장 안전하고 확장성이 좋다.
