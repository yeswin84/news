# 뉴스 기록 앱 워크플로우 기준 문서

이 문서는 `현재 완성된 뉴스 기록 앱`의 최종 흐름을 다른 버전으로 그대로 재사용하기 위한 기준 문서다.

목표는 다음과 같다.

- 구동 방식은 그대로 유지한다.
- 화면 문구, 헤드 카피, 결과 섹션 이름, 디자인 톤만 바꿔서 새 버전으로 확장한다.
- 어떤 파일은 그대로 두고, 어떤 파일만 수정하면 되는지 빠르게 판단할 수 있게 한다.

## 1. 현재 앱의 핵심 목적

이 앱은 `신문 기사 읽기 토론 수업`의 음성 파일을 업로드하면 아래 결과를 자동 생성하는 개인용 웹앱이다.

- 요약
- 핵심 포인트
- 새롭게 알게 된 개념
- 학생 기록
- 리포트 보기
- 노션용 복사
- PDF 저장
- 문서 저장(.md)
- 기록 목록 저장/열람/삭제

원본 음성은 최종 보관하지 않고, `텍스트 결과 중심`으로 기록을 남긴다.

## 2. 최종 사용자 흐름

### A. 메인 진입

사용자는 메인 화면에서 아래를 본다.

- 메인 헤드: `뉴스 기록`
- 서브헤드: `읽고 · 생각하고 · 기록하고 · 성장하는`
- 설명 문구
- 새 기록 만들기 영역
- 기존 기록 목록

### B. 새 기록 만들기

사용자는 새 기록 만들기 영역에서 다음을 수행한다.

- `파일에서 선택`으로 음성 파일 선택
- 또는 `바로 녹음`으로 녹음 후 업로드 목록에 추가
- 파일은 1개 이상 올릴 수 있음
- 여러 파일은 `한 번의 기록`으로 묶여 처리됨

지원 흐름:

- PC: 파일 선택 업로드
- 휴대폰: 파일 선택 또는 바로 녹음
- 여러 파일: 업로드 순서대로 하나의 기록으로 묶임

### C. 처리 중 상태

사용자가 `기록 만들기`를 누르면 로딩 모달이 뜬다.

표시 흐름:

- 업로드 중: 몇 번째 파일, 파일명, 업로드 퍼센트 표시
- 생성 중: 전사 + 요약 + 개념 정리 + 학생 기록 생성 안내
- 경과 시간 표시

주의:

- 음성 1개보다 2개 이상일 때 시간이 더 오래 걸릴 수 있음
- 실제 프로덕션 기준으로 2개 음성 처리에 수십 초가 걸릴 수 있음

### D. 결과 보기

생성이 끝나면 새 기록이 선택되고, 상세 영역 기본 탭은 `리포트 보기`다.

리포트 보기 구성:

- 상단 리포트 헤더
- 요약
- 핵심 포인트
- 새롭게 알게 된 개념
- 학생 기록
- 한 줄 느낌 / 생각해볼 내용

### E. 바로 복사

`바로 복사` 탭에서는 다음을 한다.

- 기록 제목 수정
- 상단 한 줄 요약 수정
- 노션용 복사 버튼 사용
- 문서 저장용 마크다운 원문 확인

중요:

- `노션용 복사` 버튼은 HTML + plain text를 함께 클립보드에 넣는다.
- 따라서 노션에 붙여넣을 때 제목, 인용문, 목록 형식이 최대한 살아난다.
- 아래 `마크다운 원문 textarea`는 저장/참고용이며, 그 내용을 직접 복사하면 노션에서 문법이 그대로 보일 수 있다.

### F. 저장

상단 버튼은 아래 두 개만 유지한다.

- `PDF 저장`
- `문서 저장`

동작:

- `PDF 저장`
  - PC: 브라우저 인쇄/PDF 흐름
  - 모바일: 실제 PDF 파일 생성 후 공유/저장 흐름
- `문서 저장`
  - `.md` 파일 다운로드
  - 전사 원문은 제외된 저장용 문서

### G. 기록 목록

기록 목록에서는 다음이 가능하다.

- 최근 기록 목록 보기
- 날짜/제목 검색
- 기록 클릭 후 다시 열람
- 각 기록별 삭제 버튼으로 삭제

## 3. 시스템 처리 흐름

### A. 프론트 시작

브라우저 로드 시:

1. `/api/config` 호출
2. `/api/lessons` 호출
3. 설정과 기존 기록 목록 렌더링

### B. 음성 업로드 방식

현재 프로덕션은 `Vercel Blob` 기반이다.

흐름:

1. 프론트에서 음성 파일 선택
2. `static/blob-upload.js`가 Blob client upload 수행
3. 업로드 완료 후 `audio_refs` 생성
4. `/api/lessons`로 JSON 요청 전송

예외 처리:

- 작은 파일은 필요 시 직접 POST fallback 가능
- 카카오/모바일 브라우저 호환을 위해 multipart 강제 사용을 피하는 조정이 들어가 있음
- Blob client는 외부 CDN이 아니라 앱 내부 번들 파일을 우선 사용함

### C. 백엔드 생성 흐름

`/api/lessons` 요청을 받으면:

1. multipart 또는 JSON 입력 파싱
2. manual transcript 또는 uploaded audio refs 확인
3. 음성이 있으면 OpenAI 전사 수행
4. 전사 결과를 바탕으로 구조화된 결과 생성
5. 최종 lesson record 저장
6. 프론트에 결과 반환

### D. 전사/생성

AI 처리 흐름:

1. 음성 로드
2. OpenAI 전사
3. 요약/핵심 포인트/개념/학생 기록 생성
4. 노션용 export text 생성
5. 저장용 record 완성

현재는 여러 음성이 들어오면 `병렬 전사`를 사용하도록 조정되어 있다.

### E. 저장 구조

현재 저장 백엔드:

- lesson record: Vercel Blob
- 임시 음성: Vercel Blob

저장되는 핵심 필드:

- lesson_date
- article_title
- transcript_text
- summary_text
- summary_points
- concept_cards
- student_summary
- lesson_headline
- notion_exports
- original_audio_names

원칙:

- 최종적으로는 텍스트 기록 중심
- 원본 음성은 장기 보관 목적이 아님

## 4. PC / 모바일 차이점

### 업로드

- PC: 파일 선택 업로드 중심
- 모바일: 파일 선택 + 바로 녹음
- 카카오/모바일 웹뷰에서는 업로드 호환성 대응이 이미 들어가 있음

### PDF 저장

- PC: `window.print()` 기반
- 모바일: `html2pdf`로 파일 생성 후 공유/저장

### 노션 복사

- 지원 브라우저에서는 rich clipboard 사용
- 일부 브라우저에서는 plain text fallback

## 5. 현재 버전을 다른 형태로 바꿀 때 수정 범위

## 5-1. 텍스트/디자인만 바꿀 때

아래 파일만 우선 수정하면 된다.

- `C:\Users\sk012\Projects\lesson_journal_app\static\index.html`
- `C:\Users\sk012\Projects\lesson_journal_app\static\app.js`
- `C:\Users\sk012\Projects\lesson_journal_app\static\styles.css`

여기서 바꾸는 것:

- 메인 헤드/서브헤드/설명 문구
- 버튼 이름
- 리포트 섹션 제목
- 복사 탭 안내 문구
- 로딩 문구
- 색상/폰트/여백/카드 스타일

## 5-2. 결과물의 구조나 말투까지 바꿀 때

아래 파일도 함께 수정해야 한다.

- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\ai.py`

여기서 바꾸는 것:

- AI 프롬프트 방향
- 결과 JSON 구조
- 요약/개념/학생 기록의 말투
- 노션 export 구조

## 5-3. 되도록 건드리지 말아야 하는 핵심 동작 파일

구동 방식 동일 유지가 목적이라면 아래 파일은 가능하면 그대로 둔다.

- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\app.py`
- `C:\Users\sk012\Projects\lesson_journal_app\lesson_journal\storage.py`
- `C:\Users\sk012\Projects\lesson_journal_app\static\blob-upload.js`
- `C:\Users\sk012\Projects\lesson_journal_app\api\upload-audio.mjs`
- `C:\Users\sk012\Projects\lesson_journal_app\api\lesson.py`
- `C:\Users\sk012\Projects\lesson_journal_app\api\lessons.py`
- `C:\Users\sk012\Projects\lesson_journal_app\api\regenerate.py`
- `C:\Users\sk012\Projects\lesson_journal_app\vercel.json`

이 파일들은 아래 핵심 로직을 담당한다.

- 생성 API
- 저장 API
- 삭제 API
- 재생성 API
- Blob 업로드
- Vercel 배포 설정
- 저장소 읽기/쓰기

## 6. 현재 완성본의 중요한 UX 원칙

- 여러 음성 파일은 `여러 수업`이 아니라 `한 수업의 여러 조각`으로 취급한다.
- 결과는 자동 생성이 기본이다.
- 학부모용 기록은 제거된 상태다.
- 학생 기록과 요약은 중복이 적게 가도록 분리되어 있다.
- 개념 정리는 관련 용어와 수업 연결까지 포함한다.
- 노션용 복사는 `바로 붙여넣기 편의`를 우선한다.
- Markdown textarea는 저장용 원문이다.
- PDF는 보기 좋은 리포트 문서형 레이아웃을 유지한다.

## 7. 새 버전으로 복제할 때 권장 순서

1. 이 기준 문서를 먼저 읽는다.
2. 새 앱의 주제와 톤을 정한다.
3. `static/index.html`에서 메인 카피를 바꾼다.
4. `static/styles.css`에서 색감/시각 톤을 바꾼다.
5. `static/app.js`에서 화면 문구와 결과 섹션 이름을 바꾼다.
6. 결과물 구조까지 달라져야 하면 `lesson_journal/ai.py`를 수정한다.
7. 업로드/저장/PDF/노션 복사는 기존 로직을 유지한다.
8. 실제 음성 1개, 2개로 테스트한다.
9. 모바일 업로드, 노션 복사, PDF 저장을 같이 확인한다.
10. 마지막에 Vercel 재배포한다.

## 8. 테스트 체크리스트

- PC에서 음성 1개 업로드 가능
- PC에서 음성 여러 개 업로드 가능
- 모바일에서 음성 선택 가능
- 모바일에서 바로 녹음 가능
- 생성 후 리포트 자동 표시
- 노션용 복사 동작 확인
- PDF 저장 동작 확인
- 문서 저장 동작 확인
- 기록 목록 저장 확인
- 기록 삭제 확인

## 9. 한 줄 결론

현재 완성본은 `신문 기사 토론 음성 -> 자동 전사 -> 요약/개념/학생 기록 생성 -> 노션 복사/PDF 저장 -> 기록 보관` 흐름으로 완성되어 있다.

다른 버전으로 확장할 때는 `핵심 구동 로직은 유지`하고, `텍스트/디자인/출력 섹션 이름` 위주로 바꾸는 것이 가장 안전하다.
